<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/admin_background"
    android:padding="@dimen/screen_padding">

    <!-- Header / Logo -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true">
        
        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@android:drawable/ic_lock_idle_lock"
            android:tint="@color/white" />
            
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/white"
            android:textSize="@dimen/body_text"
            android:layout_marginTop="4dp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:paddingHorizontal="@dimen/screen_padding">

        <!-- Raison du blocage (ex: "Le temps d'écran est terminé") -->
        <TextView
            android:id="@+id/tvBlockReason"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/lock_reason_text"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp"
            android:visibility="gone" />

        <!-- Timer (visible en mode TIMER uniquement) -->
        <TextView
            android:id="@+id/tvRemainingTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0:58:34"
            android:textSize="@dimen/lock_timer_text"
            android:textColor="@color/white"
            android:fontFamily="sans-serif-light"
            android:layout_marginBottom="32dp" />

        <!-- Heure de déblocage (ex: "Disponible demain à 15h") -->
        <TextView
            android:id="@+id/tvUnlockTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/lock_secondary_text"
            android:textColor="@color/main_text_secondary"
            android:gravity="center"
            android:layout_marginBottom="48dp"
            android:visibility="gone" />

        <!-- Input Capsule -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_code_input"
            android:layout_marginBottom="16dp">
            
            <EditText
                android:id="@+id/etCodeInput"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="••••••"
                android:textSize="@dimen/title_text"
                android:textColor="@color/white"
                android:textColorHint="@color/main_text_secondary"
                android:letterSpacing="0.4"
                android:inputType="numberPassword"
                android:maxLength="6"
                android:background="@android:color/transparent"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:cursorVisible="false"
                android:gravity="center"
                android:minWidth="220dp"
                android:imeOptions="actionDone" />
        </FrameLayout>

        <!-- Action (ex: "Demande un code à un parent") -->
        <TextView
            android:id="@+id/tvCodeLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/enter_code"
            android:textSize="@dimen/lock_secondary_text"
            android:textColor="@color/main_text_secondary"
            android:layout_marginBottom="16dp" />

        <!-- Message d'erreur / info -->
        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="@dimen/body_text"
            android:textColor="@color/status_bad"
            android:minHeight="24dp"
            android:gravity="center" />

    </LinearLayout>

    <Button
        android:id="@+id/btnAdmin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/admin_panel"
        android:textSize="@dimen/lock_secondary_text"
        android:paddingHorizontal="32dp"
        android:paddingVertical="12dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/bg_admin_button"
        android:contentDescription="@string/admin_panel"
        android:focusable="true"
        android:visibility="visible" />

</RelativeLayout>
